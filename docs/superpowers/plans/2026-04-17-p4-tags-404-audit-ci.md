# P4 — Tags + 404 + URL Audit + CI Workflow Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 補齊 P3 未交付的 `/tags/` 系列頁面與 `/404.html`，實作 URL preservation audit（`audit-links.mts` + `url-diff.mts`），建立 GitHub Actions deploy workflow（**不切 GH Pages Source**，P5 才切），通過 P4 acceptance 後銜接 P5。

**Architecture:** 本 plan 分四層：(1) **補頁面層** — `src/utils/tags.ts` pure aggregator + `src/pages/tags/index.astro` 列所有 tag + `src/pages/tags/[tag].astro` dynamic route + `src/pages/404.astro`；(2) **audit 腳本層** — `scripts/audit-links.mts` 由 5 個 pure checker（extractUrls / classifyUrl / checkBasePrefix / checkTrailingSlash / checkTargetExists）組成，每個獨立 Vitest 測試，CLI wrapper 薄層組裝；(3) **url-diff 層** — 從 `~/snapshot-pre-astro-migration-20260415.tar.gz` 抽出 legacy URL 集合到 committed `scripts/legacy-public-urls.txt`，`scripts/url-diff.mts` 用 pure filter 比對新舊；(4) **CI 層** — `.github/workflows/deploy.yml` 用 `withastro/action@v6` + `actions/deploy-pages@v4`，**保留 `gh-pages` branch 不動**（P5 才切 Source）。

**Tech Stack:**
- Astro 6（routing、`getCollection`、`getStaticPaths`）
- TypeScript strict + Vitest 3.x（`.mts` scripts）
- `node-html-parser`（**新 devDep**，audit-links 用）
- `tar`（POSIX，抽 snapshot）
- `node:fs`、`node:path`、`node:util.parseArgs`
- GitHub Actions（`withastro/action@v6`、`actions/deploy-pages@v4`）

**Spec 來源:**
- `docs/superpowers/specs/2026-04-14-hexo-to-astro-migration-design.md`（P0 設計定案）
- `docs/superpowers/plans/2026-04-15-p2-content-migration.md`（P2 plan，作為本 plan 格式對照）
- P3 merged 狀態（master tip `54e1809`）

---

## P3 繼承 context（必讀）

寫本 plan 時在 `master` 跟 P3 merged 結果對照出的上游狀態與遺留項：

1. **P3 實際交付範圍比 spec 窄**：spec 原本 P3 要出 `Tags/About/404`，實際只交付 `about.astro`。`src/pages/` 現況為 `index.astro`、`about.astro`、`rss.xml.ts`、`[year]/[month]/[day]/[slug].astro`，**無 tags、無 404**。P4 scope 擴大到補齊這兩塊。
2. **URL 保留目標**：spec 目標 #2 寫「56 篇英文 slug URL 完全保留」，目標 #3 寫「7 篇中文 slug 放棄舊 URL」。Tags 頁的 URL 保留**沒有明文**但屬於合理預期（Icarus 原 nav 有 Tags）。本 plan 把 tags 頁主 URL 列入保留範圍，tags 分頁（`/tags/*/page/N/`，共 5 個）列入 accepted loss。
3. **Tags 資料實況**：舊 `public/tags/` 共 101 個 URL，含 46 個 distinct tag。其中 10 個中文 tag（`切版`、`前端開發`、`工作心得`、`工程師思維`、`職涯`、`職涯成長`、`軟體開發`、`重構`、`開發工作流程`、`開發心得`），舊站 URL 原樣保留中文**未做 urlencode**。
4. **Tag URL encoding 未知**：Astro 6 的 `[tag].astro` dynamic route 給中文 param 時，產出 URL 是否 urlencode（`/tags/%E5%89%8D%E7%AB%AF%E9%96%8B%E7%99%BC/`）還是保留中文（`/tags/前端開發/`）**未經 literal 驗證**。這會決定 url-diff 是否 pass，**P4 第一件事要 spike 驗證**。若 Astro 強制 urlencode，需決定：(a) 接受中文 tag 的舊 URL 失守（多 10 筆 accepted loss） (b) 想辦法繞過（e.g. 在 `[tag].astro` 內用 rewrite）。
5. **`@fontsource/noto-sans-tc` 而非 `astro:fonts`**：spec 寫 `astro:fonts`，P3 實作用 `@fontsource/noto-sans-tc` package。這是 P3 scope drift，但不影響 P4 audit，**本 plan 不處理**（P5 或後續 polish 再看）。
6. **GA4 placeholder**：`BaseLayout.astro` 的 GA4 ID 是 `G-XXXXXXXXXX` placeholder（舊站是 `G-SG50KRG586`）。這是 P5 切換時的決定，**P4 不處理**。
7. **導航列要加 Tags**：`BaseLayout.astro` 目前只有 `首頁` / `About`，本 plan Task 8 實作 tags index 時順手加入。
8. **TZ-aware YAML guard** 已在 P2 建立（`src/utils/dates.ts` 的 `parseFrontmatterDate` + `formatTaipeiIso`），P4 所有與日期相關的呈現（tags 頁列 posts 的發表日期）都用 `formatDateParams` + `formatTaipeiIso`，不可直接 `.toISOString()`。
9. **`node-html-parser` 不在現有 devDeps**：需要新加。選型理由：(a) 零 runtime deps 小體積 (b) CSS selector 語法跟 cheerio 一致但快 10x (c) audit-links 只做 attribute 抽取不需完整 DOM，這個 parser 夠用。替代方案（cheerio、linkedom）都可以但 bundle 更大。
10. **Snapshot 實際路徑**：`~/snapshot-pre-astro-migration-20260415.tar.gz`（2.3M，466 entries），解開後 top-level 是 `public/`。本 plan Task 5 會 extract 到 `/tmp/legacy-public/` 並抽出 URL list。

---

## 需要早期 spike 的未知

**唯一 critical unknown：Astro 6 中文 dynamic param 的 URL 輸出格式。**

Task 2 會做一次 minimal spike（<3 分鐘）：建最小 `[tag].astro` 回一個中文 tag param、跑 `astro build`、檢查 `dist/tags/` 實際產出的目錄名。根據結果決定 Task 3 實作策略：

| Spike 結果 | 策略 |
|---|---|
| 保留中文目錄名 | 直接用 tag 原字串當 param，URL 跟舊站 1:1 |
| urlencode 成 `%E5%..` | 10 個中文 tag URL 標為 accepted loss，url-diff 加進 whitelist |
| 兩邊都產（重定向） | 選 Astro 推薦方向 |

Spike 結論會寫進 Task 2 的 commit message，後續 Task 3 依此繼續。

---

## 子決策（預設值，本 plan 全部沿用）

| # | 項目 | 預設 | 理由 |
|---|---|---|---|
| D1 | Tags 分頁 | **不做**（5 個 `/page/N/` URL 標 accepted loss） | 無 SEO 證據顯示分頁頁 indexable；Astro `paginate()` 可後續補 |
| D2 | `/tags/index.astro` 樣式 | tag cloud（tag 名 + 文章數），無 styling polish | Icarus tag cloud 是裝飾，P4 求功能可用即可 |
| D3 | `/tags/[tag].astro` 內容 | 該 tag 所有 posts 的標題、日期、description | 跟首頁文章列表一致 |
| D4 | `/404.astro` | 固定文案「404 - 找不到頁面 / 你來到了一個荒蕪之地...」+ 回首頁連結 | 對齊 Hexo 舊 404 文案 |
| D5 | Tag 展開來源 | `getCollection('posts')` 後 `flatMap(p => p.data.tags)` 去重 | schema 已保證 `tags: string[]` |
| D6 | tag param 大小寫 | 原樣保留（不 lowercase） | 舊 URL 大小寫混雜（`AI-Coding` vs `css`），lowercase 會破壞 URL |
| D7 | audit-links HTML parser | `node-html-parser` | 見 P3 繼承 context #9 |
| D8 | url-diff 來源 | `scripts/legacy-public-urls.txt`（committed） | 對照 P1 `legacy-public-dates.txt` pattern，CI 不依賴 snapshot tarball |
| D9 | workflow 觸發 | `push` to master + `workflow_dispatch` | spec 明寫 |
| D10 | workflow 不切 Source | P4 不做 `actions/deploy-pages` 的實際部署，只驗 yaml valid + 本地 `act` 不跑 | Prod 站台在 P5 前完全不動 |

**D10 補充**：P4 的 workflow yaml 會有 `actions/deploy-pages@v4` step，但 repo Settings 的 Pages Source 還是「Deploy from a branch: `gh-pages`」，所以即使 master 有 push 觸發 workflow，deploy-pages action 會 fail（因為 Source 還沒切），不會影響線上站。這是 spec 設計的 preview 策略。P4 不跑 live workflow，只檢 yaml 語法。

---

## File Structure

```
blog/                                         # master
├─ src/
│  ├─ utils/
│  │  ├─ tags.ts                              # P4 Task 7 新增
│  │  └─ tags.test.ts                         # P4 Task 7 新增
│  ├─ layouts/
│  │  └─ BaseLayout.astro                     # P4 Task 8 修改（nav 加 Tags）
│  └─ pages/
│     ├─ tags/
│     │  ├─ index.astro                       # P4 Task 8 新增
│     │  └─ [tag].astro                       # P4 Task 9 新增（Task 2 spike 後）
│     └─ 404.astro                            # P4 Task 10 新增
├─ scripts/
│  ├─ legacy-public-urls.txt                  # P4 Task 5 新增（committed）
│  ├─ audit-links.mts                         # P4 Task 11-16 新增
│  ├─ audit-links/                            # P4 Task 12-15 新增（pure modules）
│  │  ├─ extract-urls.ts                      # Task 12
│  │  ├─ classify-url.ts                      # Task 13
│  │  ├─ check-base-prefix.ts                 # Task 14
│  │  ├─ check-trailing-slash.ts              # Task 15
│  │  └─ check-target-exists.ts               # Task 16
│  ├─ url-diff.mts                            # P4 Task 18-19 新增
│  ├─ url-diff/                               # P4 Task 17-18 新增
│  │  ├─ parse-urls.ts                        # Task 17
│  │  └─ apply-filters.ts                     # Task 18
│  └─ __tests__/
│     ├─ audit-links/
│     │  ├─ extract-urls.test.mts             # Task 12
│     │  ├─ classify-url.test.mts             # Task 13
│     │  ├─ check-base-prefix.test.mts        # Task 14
│     │  ├─ check-trailing-slash.test.mts     # Task 15
│     │  ├─ check-target-exists.test.mts      # Task 16
│     │  └─ fixtures/                         # mini HTML / file tree fixtures
│     │     ├─ html/*.html
│     │     └─ tree/**
│     └─ url-diff/
│        ├─ parse-urls.test.mts               # Task 17
│        └─ apply-filters.test.mts            # Task 18
├─ .github/
│  └─ workflows/
│     └─ deploy.yml                           # P4 Task 20 新增（repo 第一次有 .github/）
├─ package.json                               # P4 修改（devDep + scripts）
└─ docs/superpowers/specs/2026-04-14-hexo-to-astro-migration-design.md
                                               # P4 Task 21 修改狀態行
```

**Module boundary：**
- Pure helpers（`scripts/audit-links/*.ts`、`scripts/url-diff/*.ts`、`src/utils/tags.ts`）**只處理資料**，不碰 `fs`/`process`
- CLI wrapper（`scripts/audit-links.mts`、`scripts/url-diff.mts`）**只**做：parseArgs、讀檔、組裝 pure helpers、呼叫 `process.exit`
- 例外：`check-target-exists.ts` 需要 `node:fs` 查檔案存在，以路徑為輸入、回 boolean，lower-level `existsSync` wrap 在內部（測試用 real fixture tree，不 mock fs）

---

## MigratedPost 型別引用

本 plan 無新型別。`getCollection('posts')` 回傳 `CollectionEntry<'posts'>[]`，`post.data` 已被 `src/content.config.ts` schema 限定為 `{ title, date, tags, description }`。

---

## Pre-flight 確認（2026-04-17 寫 plan 當下在 master 跑過）

- master tip: `a719aa0 chore: 忽略 .serena/ 與 .tmp/ tooling 本機狀態`
- 無 worktree、無 local `feat/astro-*` branch
- `origin/gh-pages` 未動，`pre-astro-migration` tag 保留於 `2fb7809`
- `src/content/posts/` 有 60 個 co-located slug 目錄，`astro build` 已在 P3 驗證通過（P3 PR #20 merge）
- `~/snapshot-pre-astro-migration-20260415.tar.gz` 存在，465+ entries，top-level `public/`
- snapshot 中 tag URL 101 筆、distinct tag 46 個、pagination 5 筆
- Node `v22.x`、npm `10.x`、astro `6.1.x`、vitest `3.x`（P2 plan Pre-flight 紀錄，P3 未改）
- `vitest.config.ts` include pattern 已涵蓋 `src/**/*.test.ts` + `scripts/**/*.test.mts`
- `.github/` 目錄不存在（`ls .github` → No such file），Task 20 要建

**Worktree 策略：**
- 建議開 `feat/astro-p4-tags-audit-ci` 從 master 開新 worktree（port 4000 或下一個可用 port）
- 跟 P2/P3 一致：plan 寫完 commit 到 master，執行另開 worktree
- 也可以直接在 master 上做，commits 在開 PR 時 review，端看 `/wt` skill 實際建議

---

## Task 0：Worktree 建立與 Plan commit（Controller 執行，不在 implementer 範圍）

> 這個 task 由 Controller（執行 plan 的人類或 orchestrator）在 kickoff 前完成，不計入 TDD tasks。

- [ ] **Step 0.1: 確認 master 狀態乾淨**

```bash
cd ~/Documents/bolaslien/blog
git status --short
# Expected: 無輸出
git log -1 --oneline
# Expected: a719aa0 或之後的 commit（本 plan 被 commit 後就是本 plan 的 commit）
```

- [ ] **Step 0.2: commit 本 plan 到 master**

```bash
git add docs/superpowers/plans/2026-04-17-p4-tags-404-audit-ci.md
git commit -m "docs(migration): 新增 P4 tags + 404 + audit + CI 實作計劃"
```

- [ ] **Step 0.3: 開 P4 worktree**

```bash
# 若有 /wt skill：
/wt 建立 worktree 執行 P4 tags + 404 + audit + CI

# 或手動：
git worktree add .worktrees/wt-4000-astro-p4-audit -b feat/astro-p4-tags-audit-ci master
cd .worktrees/wt-4000-astro-p4-audit
npm install
```

- [ ] **Step 0.4: 安裝新 devDep**

```bash
npm install --save-dev node-html-parser
```

Expected：`package.json` devDependencies 出現 `"node-html-parser": "^6.x.x"`。

- [ ] **Step 0.5: 確認 baseline 綠燈**

```bash
npm test
npm run verify:dates
npm run build
```

Expected：三個都 exit 0，`dist/` 產出含 60 篇 post 的動態路由頁。**若任一 fail 不要進 Task 1，先回報 Controller。**

- [ ] **Step 0.6: Commit devDep**

```bash
git add package.json package-lock.json
git commit -m "chore(p4): 加入 node-html-parser devDep 給 audit-links 使用"
```

---

## Task 1：Tag URL encoding spike（<5 分鐘，可拋棄）

**目標：** literal 驗證 Astro 6 的 `[tag].astro` dynamic route 吃中文 param 時，build 產出的目錄名是保留中文還是 urlencode。結論寫進 commit message，Task 2 依此繼續。

**Files:**
- Create: `src/pages/_spike-tag/[tag].astro`（spike，Task 結尾刪除）

- [ ] **Step 1.1: 建 spike 檔**

```bash
mkdir -p src/pages/_spike-tag
```

檔案 `src/pages/_spike-tag/[tag].astro`：

```astro
---
export async function getStaticPaths() {
  return [
    { params: { tag: '前端開發' }, props: { note: 'cjk' } },
    { params: { tag: 'javascript' }, props: { note: 'ascii' } },
  ];
}

const { tag } = Astro.params;
const { note } = Astro.props;
---
<p>tag: {tag} (note: {note})</p>
```

- [ ] **Step 1.2: Build**

```bash
npm run build
```

Expected：exit 0。

- [ ] **Step 1.3: 觀察實際 URL 輸出**

```bash
find dist/_spike-tag -type d | sort
```

記下輸出。三種可能：

| 輸出 | 結論 |
|---|---|
| `dist/_spike-tag/前端開發/` | 保留中文。Task 3 直接用 tag 原字串。 |
| `dist/_spike-tag/%E5%89%8D%E7%AB%AF%E9%96%8B%E7%99%BC/` | urlencode。Task 3 需 accept loss 10 個中文 tag URL。 |
| 其他（error/空） | stop，回報 Controller，調整 plan。 |

- [ ] **Step 1.4: 刪 spike**

```bash
rm -rf src/pages/_spike-tag
rm -rf dist
```

- [ ] **Step 1.5: Commit 結論**

```bash
git add -A
git commit --allow-empty -m "chore(p4): tag URL spike — Astro 6 對中文 [tag] param 輸出 <結論>"
```

**結論寫清楚**：commit message 內文必須含實際 URL 輸出、資料（e.g. `dist/_spike-tag/前端開發/index.html 存在 → 保留中文`），讓 Task 3 讀 git log 就能確認走哪條分支。

---

## Task 2：`src/utils/tags.ts` — aggregator pure helper（TDD）

**目標：** 寫 `getAllTags(posts)` 回傳 `Array<{ tag: string; count: number }>`（依 count desc、同 count 依 tag asc 排序），與 `getPostsByTag(posts, tag)` 回傳該 tag 的 posts 陣列（依 date desc）。兩者皆 pure，給 tags pages 使用。

**Files:**
- Create: `src/utils/tags.ts`
- Create: `src/utils/tags.test.ts`

- [ ] **Step 2.1: 寫失敗測試**

`src/utils/tags.test.ts`：

```ts
import { describe, it, expect } from 'vitest';
import { getAllTags, getPostsByTag } from './tags';

type FakePost = {
  id: string;
  data: { title: string; date: Date; tags: string[]; description: string };
};

function makePost(id: string, tags: string[], dateStr: string): FakePost {
  return {
    id,
    data: {
      title: id,
      date: new Date(dateStr),
      tags,
      description: '',
    },
  };
}

describe('getAllTags', () => {
  it('aggregate tag 出現次數並去重', () => {
    const posts = [
      makePost('a', ['css', 'javascript'], '2021-01-01'),
      makePost('b', ['css'], '2021-02-01'),
      makePost('c', ['javascript', 'css'], '2021-03-01'),
    ];
    expect(getAllTags(posts)).toEqual([
      { tag: 'css', count: 3 },
      { tag: 'javascript', count: 2 },
    ]);
  });

  it('count 相同時 tag 依字母 asc 排序', () => {
    const posts = [
      makePost('a', ['ab', 'cd'], '2021-01-01'),
    ];
    expect(getAllTags(posts)).toEqual([
      { tag: 'ab', count: 1 },
      { tag: 'cd', count: 1 },
    ]);
  });

  it('中文 tag 大小寫原樣保留不 lowercase', () => {
    const posts = [
      makePost('a', ['前端開發', 'AI-Coding'], '2021-01-01'),
    ];
    const tags = getAllTags(posts).map((t) => t.tag);
    expect(tags).toContain('前端開發');
    expect(tags).toContain('AI-Coding');
  });

  it('empty posts 陣列回 []', () => {
    expect(getAllTags([])).toEqual([]);
  });

  it('post 無 tags 時不崩', () => {
    const posts = [makePost('a', [], '2021-01-01')];
    expect(getAllTags(posts)).toEqual([]);
  });
});

describe('getPostsByTag', () => {
  it('只回該 tag 的 posts', () => {
    const posts = [
      makePost('a', ['css'], '2021-01-01'),
      makePost('b', ['javascript'], '2021-02-01'),
      makePost('c', ['css', 'javascript'], '2021-03-01'),
    ];
    const result = getPostsByTag(posts, 'css');
    expect(result.map((p) => p.id)).toEqual(['c', 'a']); // date desc
  });

  it('tag 不存在時回 []', () => {
    const posts = [makePost('a', ['css'], '2021-01-01')];
    expect(getPostsByTag(posts, 'nonexistent')).toEqual([]);
  });

  it('tag 大小寫 / 中文都精確匹配', () => {
    const posts = [
      makePost('a', ['前端開發'], '2021-01-01'),
      makePost('b', ['前端'], '2021-01-01'),
      makePost('c', ['AI-Coding'], '2021-01-01'),
      makePost('d', ['ai-coding'], '2021-01-01'),
    ];
    expect(getPostsByTag(posts, '前端開發').map((p) => p.id)).toEqual(['a']);
    expect(getPostsByTag(posts, 'AI-Coding').map((p) => p.id)).toEqual(['c']);
  });
});
```

- [ ] **Step 2.2: 跑測試確認 FAIL**

```bash
npm test -- src/utils/tags.test.ts
```

Expected：全部 fail（`getAllTags/getPostsByTag is not exported`）。

- [ ] **Step 2.3: 最小實作**

`src/utils/tags.ts`：

```ts
interface PostLike {
  id: string;
  data: { date: Date; tags: string[] };
}

export function getAllTags(posts: PostLike[]): Array<{ tag: string; count: number }> {
  const counts = new Map<string, number>();
  for (const post of posts) {
    for (const tag of post.data.tags) {
      counts.set(tag, (counts.get(tag) ?? 0) + 1);
    }
  }
  return [...counts.entries()]
    .map(([tag, count]) => ({ tag, count }))
    .sort((a, b) => (b.count - a.count) || a.tag.localeCompare(b.tag));
}

export function getPostsByTag<T extends PostLike>(posts: T[], tag: string): T[] {
  return posts
    .filter((p) => p.data.tags.includes(tag))
    .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());
}
```

- [ ] **Step 2.4: 跑測試確認 PASS**

```bash
npm test -- src/utils/tags.test.ts
```

Expected：全綠。

- [ ] **Step 2.5: Commit**

```bash
git add src/utils/tags.ts src/utils/tags.test.ts
git commit -m "feat(p4): tags aggregator helper（getAllTags / getPostsByTag）"
```

---

## Task 3：`src/pages/tags/index.astro` — tag cloud + `BaseLayout` nav 加 Tags

**目標：** 列所有 tag + 文章數連到各 tag 頁，`BaseLayout.astro` 的 nav 加入 Tags 連結。

**Files:**
- Create: `src/pages/tags/index.astro`
- Modify: `src/layouts/BaseLayout.astro`

- [ ] **Step 3.1: 建 tags index page**

`src/pages/tags/index.astro`：

```astro
---
import { getCollection } from 'astro:content';
import { getAllTags } from '../../utils/tags';
import BaseLayout from '../../layouts/BaseLayout.astro';

const posts = await getCollection('posts');
const tags = getAllTags(posts);
---

<BaseLayout title="標籤" description="所有文章標籤">
  <h1>標籤</h1>
  <ul class="tag-list">
    {tags.map(({ tag, count }) => (
      <li>
        <a href={`/blog/tags/${tag}/`}>{tag}</a>
        <span class="count">({count})</span>
      </li>
    ))}
  </ul>
</BaseLayout>

<style>
  .tag-list {
    list-style: none;
    padding: 0;
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem 1rem;
  }
  .count {
    color: #888;
    margin-left: 0.25rem;
  }
</style>
```

- [ ] **Step 3.2: 修 BaseLayout nav**

`src/layouts/BaseLayout.astro` 第 35-38 行：

```astro
    <nav>
      <a href="/blog/">首頁</a>
      <a href="/blog/tags/">標籤</a>
      <a href="/blog/about/">About</a>
    </nav>
```

- [ ] **Step 3.3: 本地驗證**

```bash
npm run dev
```

開瀏覽器到 `http://localhost:4321/blog/tags/`，確認：
- 列出所有 tag（46 個含中文）
- 每個 tag 有正確 count
- 每個 tag 連結到 `/blog/tags/<tag>/`（點下去會 404 因為 [tag].astro 還沒實作，正常）
- 首頁、about 也能開，nav 上有「標籤」連結

`Ctrl+C` 停 dev server。

- [ ] **Step 3.4: Commit**

```bash
git add src/pages/tags/index.astro src/layouts/BaseLayout.astro
git commit -m "feat(p4): /tags/ index 頁 + BaseLayout nav 加入標籤連結"
```

---

## Task 4：`src/pages/tags/[tag].astro` — dynamic route（依 Task 1 spike 結論）

**目標：** 為每個 tag 產出 `/blog/tags/<tag>/index.html`，列該 tag 的所有 posts（標題、日期、description）。

**Files:**
- Create: `src/pages/tags/[tag].astro`

- [ ] **Step 4.1: 實作 dynamic route**

`src/pages/tags/[tag].astro`：

```astro
---
import { getCollection } from 'astro:content';
import type { GetStaticPaths, InferGetStaticPropsType } from 'astro';
import { getAllTags, getPostsByTag } from '../../utils/tags';
import { formatDateParams, formatTaipeiIso } from '../../utils/dates';
import BaseLayout from '../../layouts/BaseLayout.astro';

export const getStaticPaths = (async () => {
  const posts = await getCollection('posts');
  const tags = getAllTags(posts);
  return tags.map(({ tag }) => ({
    params: { tag },
    props: { tag, posts: getPostsByTag(posts, tag) },
  }));
}) satisfies GetStaticPaths;

type Props = InferGetStaticPropsType<typeof getStaticPaths>;
const { tag, posts } = Astro.props as Props;
---

<BaseLayout title={`標籤：${tag}`} description={`標籤「${tag}」的所有文章`}>
  <h1>標籤：{tag}</h1>
  <p>{posts.length} 篇文章</p>
  <ul>
    {posts.map((post) => {
      const { year, month, day } = formatDateParams(post.data.date);
      const href = `/blog/${year}/${month}/${day}/${post.id}/`;
      return (
        <li>
          <a href={href}>{post.data.title}</a>
          <time datetime={formatTaipeiIso(post.data.date)}>{year}-{month}-{day}</time>
          {post.data.description && <p>{post.data.description}</p>}
        </li>
      );
    })}
  </ul>
</BaseLayout>
```

- [ ] **Step 4.2: Build + literal 檢查 tag URL 輸出**

```bash
npm run build
find dist/tags -maxdepth 1 -type d | sort
```

Expected 依 Task 1 結論：

**若 Task 1 結論是「保留中文」：**
```
dist/tags
dist/tags/AI-Agents
dist/tags/AI-Coding
dist/tags/AI-開發工具
dist/tags/AI-輔助開發
dist/tags/AI協作
...
dist/tags/軟體開發
dist/tags/重構
dist/tags/開發工作流程
dist/tags/開發心得
```

中文 tag 目錄名應為原字串。

**若 Task 1 結論是「urlencode」：**
```
dist/tags/%E5%89%8D%E7%AB%AF%E9%96%8B%E7%99%BC
...
```

任一情況下都繼續，只是 Task 18 `apply-filters` 的 whitelist 配置不同。

- [ ] **Step 4.3: 抽樣開 HTML 檢查內容**

隨機選一個 tag（e.g. `javascript`，有多篇）：

```bash
cat dist/tags/javascript/index.html | grep -oE "<title>[^<]+</title>" | head -1
grep -c '<li>' dist/tags/javascript/index.html
```

Expected：title 含「標籤：javascript」，至少 3 個 `<li>`（javascript tag 舊有 23 筆）。

- [ ] **Step 4.4: Commit**

```bash
git add src/pages/tags/[tag].astro
git commit -m "feat(p4): /tags/[tag] dynamic route 頁"
```

---

## Task 5：`src/pages/404.astro` — 404 頁

**目標：** 建靜態 404 頁，文案對齊 Hexo 舊 `404.html`。

**Files:**
- Create: `src/pages/404.astro`

- [ ] **Step 5.1: 建 404 頁**

`src/pages/404.astro`：

```astro
---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="找不到頁面" description="404 - 找不到頁面">
  <section class="not-found">
    <h1>404</h1>
    <p>你來到了一個荒蕪之地，這個頁面不存在或已被移除。</p>
    <p>
      <a href="/blog/">回首頁</a>
      <a href="/blog/tags/">查看所有標籤</a>
    </p>
  </section>
</BaseLayout>

<style>
  .not-found {
    text-align: center;
    padding: 4rem 1rem;
  }
  .not-found h1 {
    font-size: 4rem;
    margin: 0;
  }
</style>
```

- [ ] **Step 5.2: Build 驗證**

```bash
npm run build
ls dist/404.html
```

Expected：`dist/404.html` 存在。

- [ ] **Step 5.3: Commit**

```bash
git add src/pages/404.astro
git commit -m "feat(p4): /404 頁"
```

---

## Task 6：本地 build + preview 驗 tags + 404 端到端通

**目標：** 跑 `npm run build && npm run preview`，真正開瀏覽器看 tags + 404，確認無 runtime error。

- [ ] **Step 6.1: Build**

```bash
npm run build
```

Expected：exit 0，`dist/` 含 `tags/`、`404.html`。

- [ ] **Step 6.2: Preview + 手動驗證**

```bash
npm run preview
```

開瀏覽器：

- `http://localhost:4321/blog/` — 首頁顯示 60 篇
- `http://localhost:4321/blog/tags/` — tag cloud 列 46 tags
- `http://localhost:4321/blog/tags/javascript/` — 列 javascript 文章
- `http://localhost:4321/blog/tags/前端開發/`（或 urlencode 版，依 Task 1 結論）— 列該 tag 文章
- `http://localhost:4321/blog/nonexistent-page/` — 看到 404 頁

`Ctrl+C` 停 preview。

- [ ] **Step 6.3: Commit（若前面有修）**

若步驟驗證過程有發現 bug 修了，commit 修正。若全部一次過，跳過本步驟。

---

## Task 7：抽 legacy URL 集合 → `scripts/legacy-public-urls.txt`

**目標：** 從 snapshot tarball 抽出所有舊 HTML URL 路徑，過濾後寫入 committed `.txt`，給 `url-diff.mts` 用。

**Files:**
- Create: `scripts/legacy-public-urls.txt`

- [ ] **Step 7.1: 解開 snapshot 到 /tmp**

```bash
tar xzf ~/snapshot-pre-astro-migration-20260415.tar.gz -C /tmp/
ls /tmp/public | head
```

Expected：`/tmp/public/` 出現，內含 `index.html`、`2020/`、`tags/` 等。

- [ ] **Step 7.2: 抽 HTML URL 路徑寫檔**

```bash
(cd /tmp && find public -name "index.html" -type f) \
  | sed 's|^public|/blog|; s|/index.html$|/|' \
  | sort -u > scripts/legacy-public-urls.txt
```

同時處理 top-level `404.html`：

```bash
echo "/blog/404.html" >> scripts/legacy-public-urls.txt
sort -u scripts/legacy-public-urls.txt -o scripts/legacy-public-urls.txt
```

- [ ] **Step 7.3: 驗證 URL 集合規模合理**

```bash
wc -l scripts/legacy-public-urls.txt
head scripts/legacy-public-urls.txt
grep -c "^/blog/20" scripts/legacy-public-urls.txt
grep -c "^/blog/tags/" scripts/legacy-public-urls.txt
```

Expected 大致：
- Total lines：150-250（60 post URL + 46 tag + 5 pagination + archives + categories + misc）
- 以 `/blog/20` 開頭（年份）：60+ 筆
- 以 `/blog/tags/` 開頭：100+ 筆（含分頁）

記下 exact 值，Task 18 applyFilters 會需要。

- [ ] **Step 7.4: Commit**

```bash
git add scripts/legacy-public-urls.txt
git commit -m "chore(p4): snapshot legacy URL 集合抽到 scripts/legacy-public-urls.txt"
```

---

## Task 8：`audit-links/extract-urls.ts` — 抽 HTML URL（TDD）

**目標：** 從 HTML string 抽出所有 `<a href>` / `<img src>` / `<link href>` / `<script src>` 屬性值，回傳 `{ url: string; source: 'a' | 'img' | 'link' | 'script' }[]`。

**Files:**
- Create: `scripts/audit-links/extract-urls.ts`
- Create: `scripts/__tests__/audit-links/extract-urls.test.mts`

- [ ] **Step 8.1: 寫失敗測試**

`scripts/__tests__/audit-links/extract-urls.test.mts`：

```ts
import { describe, it, expect } from 'vitest';
import { extractUrls } from '../../audit-links/extract-urls';

describe('extractUrls', () => {
  it('抽 <a href>', () => {
    const html = '<html><body><a href="/blog/foo/">link</a></body></html>';
    expect(extractUrls(html)).toEqual([{ url: '/blog/foo/', source: 'a' }]);
  });

  it('抽 <img src>', () => {
    const html = '<img src="/blog/img/logo.svg" alt="">';
    expect(extractUrls(html)).toEqual([{ url: '/blog/img/logo.svg', source: 'img' }]);
  });

  it('抽 <link href> (stylesheet / preload / icon / canonical)', () => {
    const html = `
      <link rel="stylesheet" href="/blog/style.css">
      <link rel="preload" href="/blog/fonts/noto.woff2" as="font">
      <link rel="icon" href="/blog/favicon.ico">
      <link rel="canonical" href="https://example.com/blog/foo/">
    `;
    const urls = extractUrls(html).map((e) => e.url);
    expect(urls).toContain('/blog/style.css');
    expect(urls).toContain('/blog/fonts/noto.woff2');
    expect(urls).toContain('/blog/favicon.ico');
    expect(urls).toContain('https://example.com/blog/foo/');
  });

  it('抽 <script src>', () => {
    const html = '<script src="/blog/_astro/hash.js"></script>';
    expect(extractUrls(html)).toEqual([{ url: '/blog/_astro/hash.js', source: 'script' }]);
  });

  it('multiple sources 一起抽', () => {
    const html = `
      <link rel="stylesheet" href="/a.css">
      <a href="/b/">b</a>
      <img src="/c.png">
      <script src="/d.js"></script>
    `;
    expect(extractUrls(html)).toHaveLength(4);
  });

  it('忽略 inline script body（無 src）', () => {
    const html = '<script>var x = 1;</script>';
    expect(extractUrls(html)).toEqual([]);
  });

  it('忽略 <a> 無 href', () => {
    const html = '<a>text</a>';
    expect(extractUrls(html)).toEqual([]);
  });

  it('空 HTML 回空陣列', () => {
    expect(extractUrls('')).toEqual([]);
    expect(extractUrls('<html></html>')).toEqual([]);
  });

  it('handle data: URL（不崩）', () => {
    const html = '<img src="data:image/svg+xml,<svg></svg>">';
    expect(extractUrls(html)).toEqual([
      { url: 'data:image/svg+xml,<svg></svg>', source: 'img' },
    ]);
  });
});
```

- [ ] **Step 8.2: 跑測試確認 FAIL**

```bash
npm test -- scripts/__tests__/audit-links/extract-urls.test.mts
```

Expected：全 fail（module 不存在）。

- [ ] **Step 8.3: 實作**

`scripts/audit-links/extract-urls.ts`：

```ts
import { parse } from 'node-html-parser';

export interface ExtractedUrl {
  url: string;
  source: 'a' | 'img' | 'link' | 'script';
}

export function extractUrls(html: string): ExtractedUrl[] {
  const root = parse(html);
  const result: ExtractedUrl[] = [];

  for (const el of root.querySelectorAll('a[href]')) {
    result.push({ url: el.getAttribute('href')!, source: 'a' });
  }
  for (const el of root.querySelectorAll('img[src]')) {
    result.push({ url: el.getAttribute('src')!, source: 'img' });
  }
  for (const el of root.querySelectorAll('link[href]')) {
    result.push({ url: el.getAttribute('href')!, source: 'link' });
  }
  for (const el of root.querySelectorAll('script[src]')) {
    result.push({ url: el.getAttribute('src')!, source: 'script' });
  }

  return result;
}
```

- [ ] **Step 8.4: 跑測試確認 PASS**

```bash
npm test -- scripts/__tests__/audit-links/extract-urls.test.mts
```

Expected：全綠。

- [ ] **Step 8.5: Commit**

```bash
git add scripts/audit-links/extract-urls.ts scripts/__tests__/audit-links/extract-urls.test.mts
git commit -m "feat(p4): audit-links/extract-urls HTML 屬性抽取 helper"
```

---

## Task 9：`audit-links/classify-url.ts` — URL 分類（TDD）

**目標：** 分類 URL 為 `'external' | 'anchor' | 'internal' | 'data'`，讓後續 checks 知道哪些要驗。

**Files:**
- Create: `scripts/audit-links/classify-url.ts`
- Create: `scripts/__tests__/audit-links/classify-url.test.mts`

- [ ] **Step 9.1: 寫失敗測試**

```ts
import { describe, it, expect } from 'vitest';
import { classifyUrl } from '../../audit-links/classify-url';

describe('classifyUrl', () => {
  it('http(s) → external', () => {
    expect(classifyUrl('https://example.com/foo')).toBe('external');
    expect(classifyUrl('http://example.com/foo')).toBe('external');
  });

  it('protocol-relative // → external', () => {
    expect(classifyUrl('//cdn.example.com/lib.js')).toBe('external');
  });

  it('# / #anchor → anchor', () => {
    expect(classifyUrl('#top')).toBe('anchor');
    expect(classifyUrl('#')).toBe('anchor');
  });

  it('data: → data', () => {
    expect(classifyUrl('data:image/svg+xml,...')).toBe('data');
  });

  it('/blog/ 開頭 → internal', () => {
    expect(classifyUrl('/blog/2021/01/01/foo/')).toBe('internal');
    expect(classifyUrl('/blog/tags/javascript/')).toBe('internal');
  });

  it('/ 開頭但不是 /blog/ → internal（後續 checkBasePrefix 會 fail）', () => {
    expect(classifyUrl('/foo/')).toBe('internal');
    expect(classifyUrl('/assets/logo.png')).toBe('internal');
  });

  it('空字串 → anchor（退化）', () => {
    expect(classifyUrl('')).toBe('anchor');
  });

  it('mailto: tel: javascript: → external（不驗）', () => {
    expect(classifyUrl('mailto:foo@example.com')).toBe('external');
    expect(classifyUrl('tel:+886912345678')).toBe('external');
    expect(classifyUrl('javascript:void(0)')).toBe('external');
  });
});
```

- [ ] **Step 9.2: 確認 FAIL、實作、確認 PASS**

`scripts/audit-links/classify-url.ts`：

```ts
export type UrlCategory = 'external' | 'anchor' | 'internal' | 'data';

export function classifyUrl(url: string): UrlCategory {
  if (url.startsWith('data:')) return 'data';
  if (url === '' || url.startsWith('#')) return 'anchor';
  if (url.startsWith('//')) return 'external';
  if (/^[a-z][a-z0-9+\-.]*:/i.test(url)) return 'external'; // http(s):, mailto:, tel:, javascript:
  if (url.startsWith('/')) return 'internal';
  // Relative paths (e.g. "./foo", "foo") — treat as internal for now
  return 'internal';
}
```

跑：`npm test -- scripts/__tests__/audit-links/classify-url.test.mts`

- [ ] **Step 9.3: Commit**

```bash
git add scripts/audit-links/classify-url.ts scripts/__tests__/audit-links/classify-url.test.mts
git commit -m "feat(p4): audit-links/classify-url URL 分類 helper"
```

---

## Task 10：`audit-links/check-base-prefix.ts` — base prefix 檢查（TDD）

**目標：** 檢查 internal URL（以 `/` 開頭）是否以 `/blog/` 開頭。不以 `/blog/` 開頭的 internal URL fail。

**Files:**
- Create: `scripts/audit-links/check-base-prefix.ts`
- Create: `scripts/__tests__/audit-links/check-base-prefix.test.mts`

- [ ] **Step 10.1: 寫失敗測試**

```ts
import { describe, it, expect } from 'vitest';
import { checkBasePrefix } from '../../audit-links/check-base-prefix';

describe('checkBasePrefix', () => {
  it('以 /blog/ 開頭 → null（pass）', () => {
    expect(checkBasePrefix('/blog/')).toBeNull();
    expect(checkBasePrefix('/blog/2021/01/01/foo/')).toBeNull();
    expect(checkBasePrefix('/blog/_astro/hash.css')).toBeNull();
  });

  it('以 / 開頭但非 /blog/ → 回 violation message', () => {
    const result = checkBasePrefix('/foo/');
    expect(result).toMatch(/base/i);
  });

  it('以 /blog 開頭但無 trailing slash（e.g. /blogsomething）→ violation', () => {
    expect(checkBasePrefix('/blogsomething/')).not.toBeNull();
  });

  it('非 / 開頭 → 視為 relative，不檢查（回 null）', () => {
    // 上層會用 classifyUrl 過濾，但 defense-in-depth
    expect(checkBasePrefix('foo')).toBeNull();
    expect(checkBasePrefix('./foo')).toBeNull();
  });
});
```

- [ ] **Step 10.2: 確認 FAIL、實作、確認 PASS**

`scripts/audit-links/check-base-prefix.ts`：

```ts
/**
 * 檢查 internal URL 是否以 /blog/ 開頭。
 * 回 null 代表 pass，回字串代表 violation 說明。
 */
export function checkBasePrefix(url: string): string | null {
  if (!url.startsWith('/')) return null; // relative, skip
  if (url === '/blog' || url.startsWith('/blog/')) return null;
  return `base prefix violation: "${url}" should start with "/blog/"`;
}
```

跑：`npm test -- scripts/__tests__/audit-links/check-base-prefix.test.mts`

- [ ] **Step 10.3: Commit**

```bash
git add scripts/audit-links/check-base-prefix.ts scripts/__tests__/audit-links/check-base-prefix.test.mts
git commit -m "feat(p4): audit-links/check-base-prefix base path 檢查"
```

---

## Task 11：`audit-links/check-trailing-slash.ts` — trailing slash 檢查（TDD）

**目標：** 對 page route（非檔案副檔名）的 internal URL 檢查 trailing slash 存在。Astro 設定是 `trailingSlash: 'always'`。

檔案副檔名 whitelist：`.xml`、`.html`、`.png`、`.jpg`、`.jpeg`、`.gif`、`.webp`、`.svg`、`.css`、`.js`、`.mjs`、`.ico`、`.woff`、`.woff2`、`.json`、`.txt`、`.pdf`、`.mp4`、`.webm`、`.avif`。

**Files:**
- Create: `scripts/audit-links/check-trailing-slash.ts`
- Create: `scripts/__tests__/audit-links/check-trailing-slash.test.mts`

- [ ] **Step 11.1: 寫失敗測試**

```ts
import { describe, it, expect } from 'vitest';
import { checkTrailingSlash } from '../../audit-links/check-trailing-slash';

describe('checkTrailingSlash', () => {
  it('page route with trailing slash → null', () => {
    expect(checkTrailingSlash('/blog/2021/01/01/foo/')).toBeNull();
    expect(checkTrailingSlash('/blog/tags/javascript/')).toBeNull();
    expect(checkTrailingSlash('/blog/')).toBeNull();
  });

  it('page route without trailing slash → violation', () => {
    expect(checkTrailingSlash('/blog/2021/01/01/foo')).not.toBeNull();
    expect(checkTrailingSlash('/blog/tags/javascript')).not.toBeNull();
  });

  it('asset with whitelisted ext → null (trailing 不強制)', () => {
    expect(checkTrailingSlash('/blog/_astro/hash.css')).toBeNull();
    expect(checkTrailingSlash('/blog/favicon.ico')).toBeNull();
    expect(checkTrailingSlash('/blog/rss.xml')).toBeNull();
    expect(checkTrailingSlash('/blog/404.html')).toBeNull();
    expect(checkTrailingSlash('/blog/fonts/noto.woff2')).toBeNull();
  });

  it('URL 含 query string / hash → 忽略 q/h 判斷 slash', () => {
    expect(checkTrailingSlash('/blog/tags/javascript/?page=2')).toBeNull();
    expect(checkTrailingSlash('/blog/tags/javascript/#top')).toBeNull();
    expect(checkTrailingSlash('/blog/tags/javascript?page=2')).not.toBeNull();
  });
});
```

- [ ] **Step 11.2: 確認 FAIL、實作、確認 PASS**

`scripts/audit-links/check-trailing-slash.ts`：

```ts
const ASSET_EXTS = new Set([
  '.xml', '.html', '.png', '.jpg', '.jpeg', '.gif', '.webp', '.svg', '.css',
  '.js', '.mjs', '.ico', '.woff', '.woff2', '.json', '.txt', '.pdf',
  '.mp4', '.webm', '.avif',
]);

export function checkTrailingSlash(url: string): string | null {
  // 切掉 query / hash
  const pathOnly = url.split('?')[0].split('#')[0];
  // 抽副檔名
  const ext = pathOnly.match(/\.[a-z0-9]+$/i)?.[0].toLowerCase();
  if (ext && ASSET_EXTS.has(ext)) return null;
  if (pathOnly.endsWith('/')) return null;
  return `trailing slash violation: "${url}" should end with "/"`;
}
```

跑測試。

- [ ] **Step 11.3: Commit**

```bash
git add scripts/audit-links/check-trailing-slash.ts scripts/__tests__/audit-links/check-trailing-slash.test.mts
git commit -m "feat(p4): audit-links/check-trailing-slash trailingSlash always 檢查"
```

---

## Task 12：`audit-links/check-target-exists.ts` — target 存在性檢查（TDD，用 real fixture tree）

**目標：** 檢查 internal URL 的 target 檔案是否實際存在於指定 root。輸入 `(url, distRoot)`，回 null / violation。

URL → 檔案路徑規則：
- URL 是 `/blog/foo/`（dir）→ 檢查 `<distRoot>/foo/index.html`
- URL 是 `/blog/foo.css`（file）→ 檢查 `<distRoot>/foo.css`
- URL 是 `/blog/`（root dir）→ 檢查 `<distRoot>/index.html`

（`distRoot` 對應實際的 `dist/`，base prefix `/blog/` 被 strip）

**Files:**
- Create: `scripts/audit-links/check-target-exists.ts`
- Create: `scripts/__tests__/audit-links/check-target-exists.test.mts`
- Create: `scripts/__tests__/audit-links/fixtures/tree/page/index.html`（空檔 OK）
- Create: `scripts/__tests__/audit-links/fixtures/tree/style.css`（空檔 OK）
- Create: `scripts/__tests__/audit-links/fixtures/tree/index.html`（空檔 OK）

- [ ] **Step 12.1: 建 fixture tree**

```bash
mkdir -p scripts/__tests__/audit-links/fixtures/tree/page
mkdir -p scripts/__tests__/audit-links/fixtures/tree/nested
touch scripts/__tests__/audit-links/fixtures/tree/index.html
touch scripts/__tests__/audit-links/fixtures/tree/page/index.html
touch scripts/__tests__/audit-links/fixtures/tree/style.css
touch scripts/__tests__/audit-links/fixtures/tree/nested/index.html
```

- [ ] **Step 12.2: 寫失敗測試**

```ts
import { describe, it, expect } from 'vitest';
import { fileURLToPath } from 'node:url';
import { dirname, resolve } from 'node:path';
import { checkTargetExists } from '../../audit-links/check-target-exists';

const __dirname = dirname(fileURLToPath(import.meta.url));
const TREE = resolve(__dirname, 'fixtures/tree');

describe('checkTargetExists', () => {
  it('dir URL 存在（/page/）→ null', () => {
    expect(checkTargetExists('/blog/page/', TREE)).toBeNull();
  });

  it('file URL 存在（/style.css）→ null', () => {
    expect(checkTargetExists('/blog/style.css', TREE)).toBeNull();
  });

  it('root URL /blog/ → 檢查 index.html', () => {
    expect(checkTargetExists('/blog/', TREE)).toBeNull();
  });

  it('nested dir URL → null', () => {
    expect(checkTargetExists('/blog/nested/', TREE)).toBeNull();
  });

  it('dir URL 不存在 → violation', () => {
    expect(checkTargetExists('/blog/missing/', TREE)).not.toBeNull();
  });

  it('file URL 不存在 → violation', () => {
    expect(checkTargetExists('/blog/missing.css', TREE)).not.toBeNull();
  });

  it('不是 /blog/ 開頭 → 回 violation（defense-in-depth）', () => {
    expect(checkTargetExists('/foo/', TREE)).not.toBeNull();
  });

  it('URL 含 query → 切掉後檢查', () => {
    expect(checkTargetExists('/blog/page/?q=1', TREE)).toBeNull();
  });

  it('URL 含 hash → 切掉後檢查', () => {
    expect(checkTargetExists('/blog/page/#top', TREE)).toBeNull();
  });
});
```

- [ ] **Step 12.3: 確認 FAIL、實作、確認 PASS**

`scripts/audit-links/check-target-exists.ts`：

```ts
import { existsSync } from 'node:fs';
import { join } from 'node:path';

/**
 * 檢查 internal URL 的 target 檔案是否在 distRoot 下存在。
 * URL 必須以 /blog/ 開頭（上層 checkBasePrefix 已過）。
 */
export function checkTargetExists(url: string, distRoot: string): string | null {
  if (!url.startsWith('/blog/') && url !== '/blog') {
    return `target check skipped: "${url}" not under /blog/`;
  }

  const pathOnly = url.split('?')[0].split('#')[0];
  const relPath = pathOnly.replace(/^\/blog\/?/, '');

  let targetPath: string;
  if (relPath === '' || relPath.endsWith('/')) {
    targetPath = join(distRoot, relPath, 'index.html');
  } else {
    targetPath = join(distRoot, relPath);
  }

  if (!existsSync(targetPath)) {
    return `target missing: "${url}" → ${targetPath}`;
  }
  return null;
}
```

跑：`npm test -- scripts/__tests__/audit-links/check-target-exists.test.mts`

- [ ] **Step 12.4: Commit**

```bash
git add scripts/audit-links/check-target-exists.ts scripts/__tests__/audit-links/check-target-exists.test.mts scripts/__tests__/audit-links/fixtures/tree/
git commit -m "feat(p4): audit-links/check-target-exists 目標檔案存在性檢查"
```

---

## Task 13：`scripts/audit-links.mts` CLI wrapper + 跑實際 dist

**目標：** CLI wrapper 呼叫 pure helpers、組合 violations、輸出報告、設 exit code。在實際 `dist/` 上跑應 exit 0。

**Files:**
- Create: `scripts/audit-links.mts`

- [ ] **Step 13.1: Build 一次確保 `dist/` 最新**

```bash
npm run build
```

- [ ] **Step 13.2: 實作 CLI**

`scripts/audit-links.mts`：

```ts
#!/usr/bin/env tsx
/**
 * audit-links: 跑過 dist/**\/*.html，驗證所有內部連結符合：
 *   - base prefix (/blog/ 開頭)
 *   - trailing slash（page route 結尾）
 *   - target 存在（在 dist/ 下能找到）
 *
 * Usage: tsx scripts/audit-links.mts [--dist <path>]
 */
import { readFileSync } from 'node:fs';
import { glob } from 'node:fs/promises';
import { resolve, relative } from 'node:path';
import { parseArgs } from 'node:util';
import { extractUrls } from './audit-links/extract-urls.ts';
import { classifyUrl } from './audit-links/classify-url.ts';
import { checkBasePrefix } from './audit-links/check-base-prefix.ts';
import { checkTrailingSlash } from './audit-links/check-trailing-slash.ts';
import { checkTargetExists } from './audit-links/check-target-exists.ts';

const { values } = parseArgs({
  options: {
    dist: { type: 'string', default: 'dist' },
  },
});
const DIST = resolve(values.dist!);

interface Violation {
  file: string;
  url: string;
  rule: string;
  message: string;
}

const violations: Violation[] = [];
let htmlCount = 0;
let urlCount = 0;

for await (const htmlPath of glob('**/*.html', { cwd: DIST })) {
  htmlCount++;
  const fullPath = resolve(DIST, htmlPath);
  const html = readFileSync(fullPath, 'utf-8');
  const urls = extractUrls(html);

  for (const { url } of urls) {
    urlCount++;
    const category = classifyUrl(url);
    if (category !== 'internal') continue;

    const rules: Array<[string, string | null]> = [
      ['base-prefix', checkBasePrefix(url)],
      ['trailing-slash', checkTrailingSlash(url)],
      ['target-exists', checkTargetExists(url, DIST)],
    ];

    for (const [rule, msg] of rules) {
      if (msg) {
        violations.push({ file: relative(DIST, fullPath), url, rule, message: msg });
      }
    }
  }
}

console.log(`[audit-links] scanned ${htmlCount} HTML files, ${urlCount} URLs`);

if (violations.length > 0) {
  console.error(`\n❌ ${violations.length} violations:\n`);
  for (const v of violations) {
    console.error(`  [${v.rule}] ${v.file}`);
    console.error(`    url: ${v.url}`);
    console.error(`    msg: ${v.message}`);
  }
  process.exit(1);
}

console.log(`✓ audit-links passed`);
process.exit(0);
```

- [ ] **Step 13.3: 跑實際 dist**

```bash
tsx scripts/audit-links.mts
```

**預期兩種情境：**

- **Case A: 一次 exit 0** — 走 Step 13.5
- **Case B: 有 violations** — 走 Step 13.4 修掉直到 exit 0

- [ ] **Step 13.4: 如果 violations 非 0，逐條診斷**

常見類型與修法：

| Violation | 可能原因 | 修法 |
|---|---|---|
| base-prefix on `/foo/` | 某個 `.astro` 硬寫 `/foo/` 漏了 `/blog` | 改成 `/blog/foo/` |
| trailing-slash on `/blog/tag/x` | 某 href 漏了 `/` | 加上 `/` |
| target-exists on `/blog/tags/某中文 tag/` | Task 1 spike 結論是 urlencode 但 href 寫原字串 | 依 spike 結論用 `encodeURIComponent(tag)` |
| target-exists on `/blog/_astro/hash.css` | 不該發生（Vite 產的檔應存在） | 檢查 dist 有無跑 build |

每次修完跑 `npm run build && tsx scripts/audit-links.mts`。

- [ ] **Step 13.5: Commit**

```bash
git add scripts/audit-links.mts
git commit -m "feat(p4): audit-links CLI — 跑 dist 全站內部連結檢查"
```

若 Step 13.4 有改到 astro 檔案，多帶一個 commit：

```bash
git add src/
git commit -m "fix(p4): 修復 audit-links 抓到的 URL violations"
```

---

## Task 14：`url-diff/parse-urls.ts` — URL 集合正規化（TDD）

**目標：** 解析 URL 列表（from 檔案或 dist scan）回 `Set<string>`，每個 URL 正規化成 `/blog/<path>/` 格式。

**Files:**
- Create: `scripts/url-diff/parse-urls.ts`
- Create: `scripts/__tests__/url-diff/parse-urls.test.mts`

- [ ] **Step 14.1: 寫失敗測試**

```ts
import { describe, it, expect } from 'vitest';
import { parseUrlLines, normalizeUrl } from '../../url-diff/parse-urls';

describe('normalizeUrl', () => {
  it('已正常 URL 原樣回', () => {
    expect(normalizeUrl('/blog/2021/01/01/foo/')).toBe('/blog/2021/01/01/foo/');
  });

  it('去除 query / hash', () => {
    expect(normalizeUrl('/blog/foo/?q=1')).toBe('/blog/foo/');
    expect(normalizeUrl('/blog/foo/#top')).toBe('/blog/foo/');
  });

  it('trim whitespace', () => {
    expect(normalizeUrl('  /blog/foo/  ')).toBe('/blog/foo/');
  });
});

describe('parseUrlLines', () => {
  it('多行字串 → Set', () => {
    const text = [
      '/blog/foo/',
      '/blog/bar/',
      '/blog/foo/', // dup
    ].join('\n');
    const result = parseUrlLines(text);
    expect(result.size).toBe(2);
    expect(result.has('/blog/foo/')).toBe(true);
    expect(result.has('/blog/bar/')).toBe(true);
  });

  it('空行 / comment 行忽略', () => {
    const text = '/blog/foo/\n\n# comment\n/blog/bar/';
    expect(parseUrlLines(text).size).toBe(2);
  });
});
```

- [ ] **Step 14.2: 確認 FAIL、實作、PASS**

`scripts/url-diff/parse-urls.ts`：

```ts
export function normalizeUrl(url: string): string {
  return url.trim().split('?')[0].split('#')[0];
}

export function parseUrlLines(text: string): Set<string> {
  const set = new Set<string>();
  for (const line of text.split('\n')) {
    const trimmed = line.trim();
    if (!trimmed || trimmed.startsWith('#')) continue;
    set.add(normalizeUrl(trimmed));
  }
  return set;
}
```

跑測試。

- [ ] **Step 14.3: Commit**

```bash
git add scripts/url-diff/parse-urls.ts scripts/__tests__/url-diff/parse-urls.test.mts
git commit -m "feat(p4): url-diff/parse-urls URL 正規化與集合解析"
```

---

## Task 15：`url-diff/apply-filters.ts` — accepted loss 過濾（TDD）

**目標：** 從 legacy URL set 濾掉「已放棄相容」的 URL，剩下的是必須在新 dist 裡找到的。

Accepted loss 規則：
1. **7 篇中文 slug post URL**：`/blog/<yyyy>/<mm>/<dd>/<chinese-slug>/`（對應 spec 決策 B）
2. **Tags 分頁 URL**：`/blog/tags/<tag>/page/<N>/`（本 plan D1）
3. **Categories URL**：`/blog/categories/...`（spec 決策 A：廢掉 categories）
4. **Archives URL**：`/blog/archives/...`（Icarus 自動產但 Astro 站不做）
5. **Sitemap XML / manifest / robots / og 圖**：`/blog/sitemap.xml`、`manifest.json`、`robots.txt`、`img/og_image.png`（Hexo 特有，Astro 用不同路徑或不做）
6. **首頁分頁**：`/blog/page/<N>/`（Icarus 自動分頁，Astro 首頁不分頁）
7. **7 個中文 tag URL**（**只**當 Task 1 spike 結論是「urlencode」時）：`/blog/tags/<chinese-tag>/`

前 6 條是 spec 明示或決策延伸的 accepted loss。第 7 條依 Task 1 spike 決定是否 enable。

**Files:**
- Create: `scripts/url-diff/apply-filters.ts`
- Create: `scripts/__tests__/url-diff/apply-filters.test.mts`

- [ ] **Step 15.1: 寫失敗測試**

```ts
import { describe, it, expect } from 'vitest';
import { applyAcceptedLossFilters } from '../../url-diff/apply-filters';

describe('applyAcceptedLossFilters', () => {
  const CHINESE_SLUG_URLS = [
    '/blog/2020/04/29/如何做一個Line機器人/',
    '/blog/2020/08/18/實作JWT機制的登入驗證/',
    '/blog/2020/08/19/為什麼我不繼續做聖騎士PM/',
    '/blog/2020/08/28/架設Hexo部落格/',
    '/blog/2020/09/21/putty使用ssh登入遠端無須密碼/',
    '/blog/2020/10/13/Vue元件溝通-子傳父-emit/',
    '/blog/2020/11/02/HTML-把header做成template/',
  ];

  it('濾掉 7 篇中文 slug post URL', () => {
    const input = new Set([
      ...CHINESE_SLUG_URLS,
      '/blog/2021/01/01/english-slug/', // 應保留
    ]);
    const result = applyAcceptedLossFilters(input, { chineseTagEncoding: 'keep' });
    expect(result.size).toBe(1);
    expect(result.has('/blog/2021/01/01/english-slug/')).toBe(true);
  });

  it('濾掉 tags 分頁', () => {
    const input = new Set([
      '/blog/tags/javascript/',      // 保留
      '/blog/tags/javascript/page/2/', // 濾
      '/blog/tags/javascript/page/3/', // 濾
    ]);
    const result = applyAcceptedLossFilters(input, { chineseTagEncoding: 'keep' });
    expect(result.has('/blog/tags/javascript/')).toBe(true);
    expect(result.has('/blog/tags/javascript/page/2/')).toBe(false);
  });

  it('濾掉 categories / archives / hexo-specific', () => {
    const input = new Set([
      '/blog/categories/前端技術/',
      '/blog/archives/',
      '/blog/archives/2021/',
      '/blog/sitemap.xml',
      '/blog/manifest.json',
      '/blog/robots.txt',
      '/blog/img/og_image.png',
      '/blog/page/2/',
      '/blog/foo/', // 保留
    ]);
    const result = applyAcceptedLossFilters(input, { chineseTagEncoding: 'keep' });
    expect(result.size).toBe(1);
    expect(result.has('/blog/foo/')).toBe(true);
  });

  it('chineseTagEncoding = "encoded" 時濾掉中文 tag URL', () => {
    const input = new Set([
      '/blog/tags/前端開發/',
      '/blog/tags/javascript/',
    ]);
    const result = applyAcceptedLossFilters(input, { chineseTagEncoding: 'encoded' });
    expect(result.size).toBe(1);
    expect(result.has('/blog/tags/javascript/')).toBe(true);
  });

  it('chineseTagEncoding = "keep" 時保留中文 tag URL', () => {
    const input = new Set([
      '/blog/tags/前端開發/',
      '/blog/tags/javascript/',
    ]);
    const result = applyAcceptedLossFilters(input, { chineseTagEncoding: 'keep' });
    expect(result.size).toBe(2);
  });

  it('英文 post / 英文 tag / /blog/ / /blog/about/ 永遠保留', () => {
    const input = new Set([
      '/blog/',
      '/blog/about/',
      '/blog/tags/',
      '/blog/tags/javascript/',
      '/blog/2021/01/01/foo-bar/',
    ]);
    const result = applyAcceptedLossFilters(input, { chineseTagEncoding: 'keep' });
    expect(result.size).toBe(5);
  });
});
```

- [ ] **Step 15.2: 確認 FAIL、實作、PASS**

`scripts/url-diff/apply-filters.ts`：

```ts
export interface FilterOptions {
  /** 'keep' = Astro 保留中文 tag URL，'encoded' = Astro urlencode → 中文 tag URL 列入 accepted loss */
  chineseTagEncoding: 'keep' | 'encoded';
}

const CHINESE_SLUG_DATES: ReadonlyArray<[string, string]> = [
  ['/blog/2020/04/29/', '如何做一個Line機器人'],
  ['/blog/2020/08/18/', '實作JWT機制的登入驗證'],
  ['/blog/2020/08/19/', '為什麼我不繼續做聖騎士PM'],
  ['/blog/2020/08/28/', '架設Hexo部落格'],
  ['/blog/2020/09/21/', 'putty使用ssh登入遠端無須密碼'],
  ['/blog/2020/10/13/', 'Vue元件溝通-子傳父-emit'],
  ['/blog/2020/11/02/', 'HTML-把header做成template'],
];

function isChineseSlugPost(url: string): boolean {
  return CHINESE_SLUG_DATES.some(([prefix, slug]) => url === `${prefix}${slug}/`);
}

function hasChineseChar(str: string): boolean {
  return /[\u4e00-\u9fff]/.test(str);
}

export function applyAcceptedLossFilters(
  urls: Set<string>,
  opts: FilterOptions,
): Set<string> {
  const result = new Set<string>();
  for (const url of urls) {
    // 1. 7 篇中文 slug post
    if (isChineseSlugPost(url)) continue;
    // 2. Tags 分頁
    if (/^\/blog\/tags\/.+\/page\/\d+\/?$/.test(url)) continue;
    // 3. Categories
    if (url.startsWith('/blog/categories/')) continue;
    // 4. Archives
    if (url === '/blog/archives/' || url.startsWith('/blog/archives/')) continue;
    // 5. Hexo-specific
    if (url === '/blog/sitemap.xml') continue;
    if (url === '/blog/manifest.json') continue;
    if (url === '/blog/robots.txt') continue;
    if (url === '/blog/img/og_image.png') continue;
    // 6. 首頁分頁
    if (/^\/blog\/page\/\d+\/?$/.test(url)) continue;
    // 7. 中文 tag URL（依 opts）
    if (opts.chineseTagEncoding === 'encoded' && /^\/blog\/tags\/[^/]+\/?$/.test(url)) {
      const tag = url.replace(/^\/blog\/tags\//, '').replace(/\/$/, '');
      if (hasChineseChar(tag)) continue;
    }
    result.add(url);
  }
  return result;
}
```

跑測試。

- [ ] **Step 15.3: Commit**

```bash
git add scripts/url-diff/apply-filters.ts scripts/__tests__/url-diff/apply-filters.test.mts
git commit -m "feat(p4): url-diff/apply-filters accepted loss 過濾規則"
```

---

## Task 16：`scripts/url-diff.mts` CLI wrapper + 跑實際 dist

**目標：** CLI 整合：讀 `scripts/legacy-public-urls.txt` → 濾 → 掃 `dist/` → 比對 → 報告 missing URL。

依 Task 1 spike 結論，CLI 用 `--chinese-tag <keep|encoded>` flag 切換（預設 `keep`，因為 spike 通常保留中文）。

**Files:**
- Create: `scripts/url-diff.mts`

- [ ] **Step 16.1: 實作 CLI**

`scripts/url-diff.mts`：

```ts
#!/usr/bin/env tsx
/**
 * url-diff: 比對 Hexo snapshot 的 legacy URL 集合與目前 dist/ 產出的 URL 集合。
 *
 * 輸入：
 *   - scripts/legacy-public-urls.txt（committed）
 *   - dist/**\/*.html
 *
 * 輸出：
 *   - 濾掉 accepted loss 後若有 legacy URL 在 dist 找不到 → exit 1
 *
 * Usage: tsx scripts/url-diff.mts [--dist <path>] [--chinese-tag keep|encoded]
 */
import { readFileSync } from 'node:fs';
import { glob } from 'node:fs/promises';
import { resolve } from 'node:path';
import { parseArgs } from 'node:util';
import { parseUrlLines, normalizeUrl } from './url-diff/parse-urls.ts';
import { applyAcceptedLossFilters } from './url-diff/apply-filters.ts';

const { values } = parseArgs({
  options: {
    dist: { type: 'string', default: 'dist' },
    'chinese-tag': { type: 'string', default: 'keep' },
    legacy: { type: 'string', default: 'scripts/legacy-public-urls.txt' },
  },
});

const DIST = resolve(values.dist!);
const LEGACY_FILE = resolve(values.legacy!);
const chineseTagEncoding = values['chinese-tag'] as 'keep' | 'encoded';

if (!['keep', 'encoded'].includes(chineseTagEncoding)) {
  console.error(`--chinese-tag must be "keep" or "encoded", got "${chineseTagEncoding}"`);
  process.exit(2);
}

// 1. Legacy
const legacyRaw = parseUrlLines(readFileSync(LEGACY_FILE, 'utf-8'));
const legacyRequired = applyAcceptedLossFilters(legacyRaw, { chineseTagEncoding });

// 2. 新 dist：掃所有 index.html + 頂層 .html
const distUrls = new Set<string>();
for await (const htmlPath of glob('**/*.html', { cwd: DIST })) {
  let url: string;
  if (htmlPath === 'index.html') {
    url = '/blog/';
  } else if (htmlPath.endsWith('/index.html')) {
    url = '/blog/' + htmlPath.replace(/\/index\.html$/, '/');
  } else {
    url = '/blog/' + htmlPath;
  }
  distUrls.add(normalizeUrl(url));
}

// 3. Diff
const missing: string[] = [];
for (const url of legacyRequired) {
  if (!distUrls.has(url)) {
    missing.push(url);
  }
}

console.log(
  `[url-diff] legacy=${legacyRaw.size} → required=${legacyRequired.size}, dist=${distUrls.size}`,
);

if (missing.length > 0) {
  console.error(`\n❌ ${missing.length} URL 在 dist 中找不到：\n`);
  for (const url of missing.sort()) console.error(`  ${url}`);
  process.exit(1);
}

console.log(`✓ url-diff passed — 所有保留 URL 都在 dist 中`);
process.exit(0);
```

- [ ] **Step 16.2: 跑實際 dist**

```bash
npm run build  # 確保 dist 是最新
tsx scripts/url-diff.mts --chinese-tag <keep 或 encoded>  # 依 Task 1 spike 結論
```

**預期兩種情境：**
- **Case A: exit 0** — 走 Step 16.4
- **Case B: 有 missing URL** — 走 Step 16.3 診斷

- [ ] **Step 16.3: 診斷 missing URL 並修**

| Missing URL pattern | 原因 | 修法 |
|---|---|---|
| `/blog/2021/.../english-slug/` | Post URL 產錯（時區 / slug） | 檢查 `src/pages/[year]/[month]/[day]/[slug].astro` 的 getStaticPaths |
| `/blog/tags/<tag>/` | Tag 寫對但 path 沒產 | 檢查 Task 4 實作 |
| `/blog/404.html` | 沒產 404 | 檢查 Task 5 |
| `/blog/about/` | about 沒產或路徑錯 | 檢查 `src/pages/about.astro` |

每次修完：`npm run build && tsx scripts/url-diff.mts --chinese-tag <mode>`。

- [ ] **Step 16.4: Commit**

```bash
git add scripts/url-diff.mts
git commit -m "feat(p4): url-diff CLI — legacy snapshot vs dist URL 保留驗證"
```

若 Step 16.3 有改 astro：

```bash
git add src/
git commit -m "fix(p4): 修復 url-diff 抓到的 URL 缺失"
```

---

## Task 17：npm scripts

**目標：** 把兩個 audit 腳本加進 `package.json`，方便 CI workflow 統一用 `npm run` 呼叫。

**Files:**
- Modify: `package.json`

- [ ] **Step 17.1: 修 package.json**

找到 `"scripts"` block，加入兩個：

```json
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify:dates": "tsx scripts/verify-dates.mts",
    "migrate:posts": "tsx scripts/migrate-posts.mts",
    "audit:links": "tsx scripts/audit-links.mts",
    "audit:url-diff": "tsx scripts/url-diff.mts",
    "audit:all": "npm run audit:links && npm run audit:url-diff"
  }
```

**注意**：`audit:url-diff` 不在這裡帶 `--chinese-tag`，由呼叫端傳；CI workflow 在 Task 18 會帶。

- [ ] **Step 17.2: 驗證**

```bash
npm run audit:links
npm run audit:url-diff -- --chinese-tag <keep 或 encoded>
npm run audit:all
```

三個都應 exit 0（第三個依前兩個）。

- [ ] **Step 17.3: Commit**

```bash
git add package.json
git commit -m "chore(p4): 加入 audit:links / audit:url-diff / audit:all npm scripts"
```

---

## Task 18：`.github/workflows/deploy.yml`

**目標：** 建 GitHub Actions workflow，build + audit + deploy-pages。**不切** repo Pages Source（P5 才切）。

**Files:**
- Create: `.github/workflows/deploy.yml`

- [ ] **Step 18.1: 建目錄與 yaml**

```bash
mkdir -p .github/workflows
```

`.github/workflows/deploy.yml`：

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run unit tests
        run: npm test

      - name: Verify dates
        run: npm run verify:dates

      - name: Build
        run: npm run build

      - name: Audit links
        run: npm run audit:links

      - name: Audit URL diff
        run: npm run audit:url-diff -- --chinese-tag <依 Task 1 結論填 keep 或 encoded>

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

**填 `--chinese-tag` 值時**：依 Task 1 spike 結論替換 `<依 Task 1 結論填 keep 或 encoded>`。

- [ ] **Step 18.2: 本地 lint yaml 語法**

```bash
# 用 Node 內建 parse 當簡單 sanity check
node -e "require('fs').readFileSync('.github/workflows/deploy.yml','utf-8')" && echo "file readable"
```

若有 `yq`：

```bash
yq '.' .github/workflows/deploy.yml > /dev/null && echo "yaml valid"
```

- [ ] **Step 18.3: Review yaml 項目 checklist**

逐項對 spec 確認（**literal 比對，不用 PASS/OK 等 verdict**）：

- [ ] `on.push.branches` 有 `master`
- [ ] `on.workflow_dispatch` 存在
- [ ] `permissions.pages` 是 `write`
- [ ] `permissions.id-token` 是 `write`
- [ ] Node 版本 `'22'`（對齊 Pre-flight 確認）
- [ ] Build step 之前有 test + verify:dates
- [ ] Build 之後有 audit:links + audit:url-diff
- [ ] Upload artifact path 是 `./dist`
- [ ] Deploy job `needs: build`
- [ ] 用 `actions/deploy-pages@v4` 而非 `peaceiris/actions-gh-pages`

任一項不符 → 修正後重 lint。

- [ ] **Step 18.4: Commit**

```bash
git add .github/workflows/deploy.yml
git commit -m "feat(p4): GitHub Actions deploy.yml（不切 Pages Source，P5 才切）"
```

---

## Task 19：更新 spec 狀態行 + P4 完成 marker

**目標：** spec 檔開頭的「狀態」行標為「P4 完成 — 進入 P5」，phasing 表加完成標記。

**Files:**
- Modify: `docs/superpowers/specs/2026-04-14-hexo-to-astro-migration-design.md`

- [ ] **Step 19.1: 找到狀態行**

```bash
grep -n "^- \*\*狀態" docs/superpowers/specs/2026-04-14-hexo-to-astro-migration-design.md
```

Expected：一行，目前寫「P2 完成 — 進入 P3（routing + layouts + fonts）」或「P3 完成 — 進入 P4」。

- [ ] **Step 19.2: 改成 P4 完成**

Edit 該行為：

```markdown
- **狀態**：P4 完成 — 進入 P5（切 GH Pages Source + prod 驗證 + 清 Hexo 檔案）
```

- [ ] **Step 19.3: Commit**

```bash
git add docs/superpowers/specs/2026-04-14-hexo-to-astro-migration-design.md
git commit -m "docs(migration): 更新 spec 狀態 — P4 完成，進入 P5"
```

---

## Task 20：最終 acceptance — 全綠

**目標：** 在 worktree 內跑完整驗證鏈，確認所有 gates 通過，working tree 乾淨。

- [ ] **Step 20.1: 全套驗證**

```bash
npm ci                                              # 乾淨 install
npm test                                            # Unit tests
npm run verify:dates                                # P1 遺產
npm run build                                       # Astro build
npm run audit:links                                 # P4 check #1
npm run audit:url-diff -- --chinese-tag <mode>      # P4 check #2
```

Expected：全部 exit 0。

- [ ] **Step 20.2: 確認 working tree 乾淨**

```bash
git status --short
```

Expected：無輸出。

- [ ] **Step 20.3: 確認 commit history**

```bash
git log --oneline master..HEAD
```

Expected：~20 個 commits，含每個 Task 的 feat / test / chore / docs commit。

- [ ] **Step 20.4: 合併前最後一次 dogfood**

```bash
npm run preview &
# 開瀏覽器，抽樣開：
#  - /blog/
#  - /blog/tags/
#  - /blog/tags/javascript/
#  - /blog/tags/前端開發/  (或 urlencoded 版)
#  - /blog/404  (試無效 URL)
#  - /blog/2021/01/01/<隨機 english slug>/
kill %1
```

每個都要能開得開、內容正確、無 console error。

**Reporter**：列實際觀察到的（標題、連結數量、console error 有無），不下 PASS/OK 評語。Any runtime error → stop，回報 Controller。

---

## P4 Acceptance（與 spec Phasing 表對照）

| Spec 要求 | 本 plan 產出 | 驗收方式 |
|---|---|---|
| 本地 `npm run build` | Task 6、Task 20 | `npm run build` exit 0 |
| `audit-links.mjs` 通過 | Task 13（改名 `.mts`） | `npm run audit:links` exit 0 |
| `url-diff.mjs` 通過 | Task 16（改名 `.mts`） | `npm run audit:url-diff --chinese-tag <mode>` exit 0 |
| 撰寫 GH Actions workflow | Task 18 | yaml 存在、Review checklist 全過 |
| 尚未切 GH Pages source | （不做事） | repo Settings → Pages → Source 維持 `gh-pages` branch |

**P4 不做**：
- 不跑 live workflow（push 觸發會 fail 在 deploy-pages step 因為 Source 還沒切，不影響線上）
- 不動 `origin/gh-pages`
- 不動 `pre-astro-migration` tag
- 不清 Hexo 檔案（P5 做）

---

## Self-Review（寫 plan 後 reviewer 用）

- [ ] **Spec coverage**：本 plan 對到 spec Phasing 表 P4 所有項目？Tags/404 是 P3 補債，已納 Task 2-5。audit 腳本 Task 8-16。Workflow Task 18。
- [ ] **Placeholder scan**：無「TBD」「implement later」。Task 1 spike 的結論在 Task 18 有顯式「<依 Task 1 結論填...>」的 placeholder — 這是 **intentional**，因為 spike 結果決定該值，且本 plan 有清楚指引怎麼填。
- [ ] **Type consistency**：
  - `extractUrls(html)` → `ExtractedUrl[]`（`{ url, source }`）— 一致用於 Task 8 / Task 13
  - `classifyUrl(url)` → `UrlCategory`（`'external' | 'anchor' | 'internal' | 'data'`）— Task 9 / Task 13
  - `checkXxx(url)` → `string | null` — Task 10/11/12/13 統一
  - `parseUrlLines(text)` / `normalizeUrl(url)` — Task 14/16 一致
  - `applyAcceptedLossFilters(urls, opts)` 的 `opts.chineseTagEncoding: 'keep' | 'encoded'` — Task 15/16/18 一致

---

## 執行建議

**Subagent-driven recommended**：Task 1 spike、Task 8-16 的 TDD 紀律、Task 18 的 yaml review 都是獨立顆粒，subagent 跑一個 task 回來 review 一次比較乾淨。

Inline 也可以，但 P4 比 P2 小（20 tasks vs 17），且有 Task 1 的早期 spike 需要 commit + 續作，batch 跑時 controller 要注意 spike 結論注入後續 task。
